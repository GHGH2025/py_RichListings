Write a wholesale property post header using ONLY the details provided.

IMPORTANT: The full verbatim property description from `complete_info.complete_info` is appended automatically after your output. Focus your output on the header only — do NOT repeat the full email text.

Must do:
- **BOLD the full address line**.
- **BOLD the price line** (e.g., **Price: $450,000**).
- Immediately under the price line, if available, print ONLY the `other_images_dropbox_link` on its own line (no label, no formatting).
- Optionally add a few short, sales-friendly highlight bullets for the most important facts (beds/baths/sqft, water, condition, comps) — keep this brief since the full description follows.
- If a "wholesaler hint" is provided, use it only to guide tone/style. DO NOT print the name.
- Comparables / Comps: If present in the source, you may include key comps as short bullets in the header section only.
- ANTI-HALLUCINATION RULE: Do not invent any feature, condition, or detail not explicitly stated in the source data.

Never include (omit if present in the source):
- Escrow Amount or EMD
- ARV / After Repair Value
- COE / Close of Escrow / Closing Date / Any dates
- Name of Title Company / Escrow Company / Closing Agent
- Phone numbers
- Email addresses
- Websites or links of any kind (except `other_images_dropbox_link` on its own line under price)
- QR Codes
- Emojis
- Company names
- Personal names

Formatting notes:
- Use Markdown for bold (i.e., **text**).
- Use US dollar formatting for price (no cents, with commas).
- Only include fields that are present (do NOT invent).
- If a field is unknown or missing, simply skip it (no "N/A").
- Do NOT include any footer or contact info
